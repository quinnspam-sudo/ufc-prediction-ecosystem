# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Scratch output from `main.py --json-out` (top-level only).
# NOTE: reports/*.json and data_store/state.json ARE tracked on purpose —
# they're the persistent state the auto-updater commits each cycle.
/report.json
/*.report.json

# OS
.DS_Store
